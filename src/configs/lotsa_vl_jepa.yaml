experiment_name: "ts_vl_jepa_lotsa"
seed: 42
device: "auto"

model:
  model_name: "ts_vl_jepa_lotsa"
  rp_encoder: "upper_tri"
  input_dim: 64
  model_dim: 256
  embedding_dim: 128
  depth: 8
  state_dim: 16
  conv_kernel: 4
  expand_factor: 1.5
  dropout: 0.05
  pooling: "mean"
  proj_dim: 256
  pred_dim: 128
  rp_mv_strategy: "mean"

data:
  dataset_type: "lotsa"
  batch_size: 256
  val_batch_size: 128
  num_workers: 4
  pin_memory: true
  val_ratio: 0.1

  cronos_kwargs:
    context_length: 96
    cache_dir: "/lustre/nobackup/WUR/AIN/stiva001/hf_datasets"
    normalize_per_series: true
    force_offline: true
    drop_last: true
    two_views: false

training:
  epochs: 200
  learning_rate: 0.0003
  weight_decay: 0.0001
  noise_std: 0.05
  ema_tau: 0.996
  max_grad_norm: 1.0
  warmup_epochs: 10
  use_amp: false

logging:
  checkpoint_dir: "../../checkpoints/vl_jepa"
  save_best_only: true
  save_last: true
